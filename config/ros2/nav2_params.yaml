# Nav2 params — G1 adapted from nav2_bringup/params/nav2_params.yaml
#
# G1 changes summary:
#   robot_base_frame: base_link -> body        (G1 body frame)
#   odom_topic: /odom -> /Odometry_loc          (FAST-LIO topic)
#   controller: DWB -> RegulatedPurePursuit     (biped: turn-then-walk, less oscillation)
#   robot_radius: 0.22 -> 0.3                   (G1 larger footprint)
#   max_vel: 0.26 -> 0.4 m/s                    (G1 max walk speed)
#   use_sim_time: True -> false                 (real hardware)
#   amcl: removed (G1 uses open3d_loc ICP localization)
#

bt_navigator:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    global_frame: map
    robot_base_frame: body                                  # G1: body frame (original: base_link)
    odom_topic: /Odometry_loc                               # G1: FAST-LIO odometry (original: /odom)
    bt_loop_duration: 10
    default_server_timeout: 20
    wait_for_service_timeout: 1000
        bt_loop_duration: 10
    default_server_timeout: 20
    wait_for_service_timeout: 1000
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node
      - nav2_is_battery_charging_condition_bt_node

bt_navigator_navigate_through_poses_rclcpp_node:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware

bt_navigator_navigate_to_pose_rclcpp_node:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware

controller_server:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["general_goal_checker"]
    controller_plugins: ["FollowPath"]

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 10.0
    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.25
    # G1: Replaced DWB with Regulated Pure Pursuit for biped walking
    # Reason: DWB produces alternating back/turn commands that cause G1 oscillation.
    # Pure Pursuit separates rotation from translation: turn first, then walk straight.
    FollowPath:
      # original: plugin: "dwb_core::DWBLocalPlanner"
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      desired_linear_vel: 0.3                               # G1: comfortable walk speed
      lookahead_dist: 0.5
      max_linear_vel: 0.4                                   # G1: max walk speed (original DWB: 0.26)
      min_linear_vel: 0.1
      max_angular_vel: 0.5
      rotate_to_heading_angular_vel: 0.3
      goal_dist_tol: 0.3
      transform_tolerance: 0.5

planner_server:
  ros__parameters:
    expected_planner_frequency: 20.0
    use_sim_time: false                                     # G1: real hardware
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_navfn_planner/NavfnPlanner"
      tolerance: 0.5
      use_astar: true                                       # G1: A* better for cluttered indoor (original: false)
      allow_unknown: true

smoother_server:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    smoother_plugins: ["simple_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: True

behavior_server:
  ros__parameters:
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "drive_on_heading", "assisted_teleop", "wait"]
    spin:
      plugin: "nav2_behaviors/Spin"
    backup:
      plugin: "nav2_behaviors/BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors/DriveOnHeading"
    wait:
      plugin: "nav2_behaviors/Wait"
    assisted_teleop:
      plugin: "nav2_behaviors/AssistedTeleop"
    global_frame: odom
    robot_base_frame: body                                  # G1: body frame (original: base_link)
    transform_tolerance: 0.1
    use_sim_time: false                                     # G1: real hardware
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 3.2

waypoint_follower:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200

velocity_smoother:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    smoothing_frequency: 20.0
    scale_velocities: False
    feedback: "OPEN_LOOP"
    max_velocity: [0.4, 0.0, 0.5]                          # G1: max walk speed (original: [0.26, 0.0, 1.0])
    min_velocity: [-0.4, 0.0, -0.5]                        # G1 (original: [-0.26, 0.0, -1.0])
    max_accel: [2.5, 0.0, 3.2]
    max_decel: [-2.5, 0.0, -3.2]
    odom_topic: "/Odometry_loc"                             # G1: FAST-LIO (original: "odom")
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]
    velocity_timeout: 1.0

# ── Costmaps ──────────────────────────────────────────────

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 0.5                                # G1: lower for WiFi bandwidth
      global_frame: map
      robot_base_frame: body                                # G1: body frame (original: base_link)
      use_sim_time: false                                   # G1: real hardware
      robot_radius: 0.3                                     # G1: larger footprint (original: 0.22)
      resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "inflation_layer"]
      # G1: removed obstacle_layer — no laser scan on G1 global costmap
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: true
        map_topic: /map                                     # G1: use /map (open3d_loc publishes here)
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 1.0                               # G1: larger safety margin (original: 0.55)
      always_send_full_costmap: True

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: map                                     # G1: map frame for consistency with localization
      robot_base_frame: body                                # G1: body frame (original: base_link)
      use_sim_time: false                                   # G1: real hardware
      rolling_window: true
      width: 3
      height: 3
      resolution: 0.05
      robot_radius: 0.3                                     # G1: larger footprint (original: 0.22)
      plugins: ["voxel_layer", "inflation_layer"]
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.304                            # G1: match inscribed radius to avoid collision (original: 0.55)
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        publish_voxel_map: True
        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 16
        max_obstacle_height: 2.0
        mark_threshold: 0
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      always_send_full_costmap: True

map_server:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    yaml_filename: "/root/maps/accumulated_grid.yaml"       # G1: path to 2D grid map

map_saver:
  ros__parameters:
    use_sim_time: false                                     # G1: real hardware
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: True
